import React from "react";
import styled from "styled-components";

const CourtCreateWrapper = styled.button``;

export const CourtCreate: React.FC = () => {
  return <CourtCreateWrapper></CourtCreateWrapper>;
};
